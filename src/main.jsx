import React from "react";
import ReactDOM from "react-dom/client";
import PassaporteDigitalApp from "./PassaporteDigitalApp";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PassaporteDigitalApp />
  </React.StrictMode>
);
